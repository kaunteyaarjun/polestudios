import ServicePageTemplate from "../../components/ServicePageTemplate";

export const metadata = {
    title: "Branding & Design | Pole Studios",
    description: "Identity, UI/UX, and Brand Strategy."
};

export default function BrandingPage() {
    return (
        <ServicePageTemplate
            title="Branding"
            subtitle="// IDENTITY & UI/UX"
            description="We build brands that command attention. From visual identity systems to immersive UI/UX design, we craft the visual language of your business."
        />
    );
}
